<resources xmlns:tools="http://schemas.android.com/tools">
    <!--沉浸式状态栏-->
    <style name="TranslucentTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <!-- 适配android手机系统8.0(api26)，Only fullscreen opaque activities can request orientation -->
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowDisablePreview">true</item>
        <item name="android:windowTranslucentNavigation">false</item> <!--属性值设为false 可设置虚拟按键区域颜色-->
        <item name="android:navigationBarColor" tools:targetApi="lollipop">@android:color/white</item>
        <item name="android:statusBarColor" tools:targetApi="lollipop">@android:color/transparent</item>
    </style>
</resources>
